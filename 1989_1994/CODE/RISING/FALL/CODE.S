;	LIST
;	OR	D0,(A0)	8150
;N	SET	8
;	REPT	18
;	MOVE	D1,N(A0)	3141
;N	SET	N+8
;	ENDR
;	OR	D2,N(A0)	8568
;	RTS
	
	LEA	CODE,A0
	LEA	TABLE,A1
	MOVE	#17,D7
	MOVE	#18,D6
DOCODE	
	MOVE.L	A0,(A1)+

	MOVE.L	#8,D0
	SUBQ	#1,D6
	MOVE	D6,D1
	MOVE	#$8150,(A0)+
DOMOVE	MOVE	#$3141,(A0)+
	MOVE	D0,(A0)+
	ADDQ	#8,D0
	DBF	D1,DOMOVE
	MOVE	#$8568,(A0)+
	MOVE	D0,(A0)+
	MOVE	#$4E75,(A0)+
	DBF	D7,DOCODE

	MOVE.L	A0,(A1)+
	MOVE.L	#8,D0
	MOVE	#$8150,(A0)+
	MOVE	#$8568,(A0)+
	MOVE	D0,(A0)+
	MOVE	#$4E75,(A0)+

	MOVE.L	A0,(A1)+
	MOVE.L	#8,D0
	MOVE	#$C240,(A0)+
	MOVE	#$8350,(A0)+
	MOVE	#$4E75,(A0)+
	NOP

	BSS
TABLE	DS.L	20
CODE	DS.B	8000