X1	EQU	0
Y1	EQU	2
X2	EQU	4
Y2	EQU	6
X3	EQU	8
Y3	EQU	10
X4	EQU	12
Y4	EQU	14

	LEA	t_points,a0
	MOVE	2(A0),D6	Y1
	CMP	2+4(A0),D6
	BGT.S	D6_WIN
;Y2>Y1	CAS 1 ET 2

	LEA	TAB,A3
	MOVE	(A0),D0
	MOVE	4(A0),D2
	MOVE	2(A0),D1
	MOVE	6(A0),D3
	JSR	CALCUL

	MOVE	Y2(A0),D6
	CMP	Y3(A0),D6
	BGT.S	D6_W
;Y3>Y2	CAS 1
	LEA	TAB,A3
	MOVE	Y3(A0),D3
	MOVE	X3(A0),D2
	MOVE	Y2(A0),D1
	MOVE	X2(A0),D0
	JSR	CALCUL
	LEA	TAB2,A3
	MOVE	Y3(A0),D3
	MOVE	X3(A0),D2
	MOVE	Y4(A0),D1
	MOVE	X4(A0),D0
	JSR	CALCUL

	LEA	TAB2,A3
	MOVE	Y4(A0),D3
	MOVE	X4(A0),D2
	MOVE	Y1(A0),D1
	MOVE	X1(A0),D0
	JSR	CALCUL
	BRA.S	DONE

D6_W	;Y2>Y3 CAS 2
	LEA	TAB2,A3
	MOVE	Y2(A0),D3
	MOVE	X2(A0),D2
	MOVE	Y3(A0),D1
	MOVE	X3(A0),D0
	JSR	CALCUL
	
	LEA	TAB2,A3
	MOVE	Y3(A0),D3
	MOVE	X3(A0),D2
	MOVE	Y4(A0),D1
	MOVE	X4(A0),D0
	JSR	CALCUL

	LEA	TAB,A3
	MOVE	Y1(A0),D3
	MOVE	X1(A0),D2
	MOVE	Y4(A0),D1
	MOVE	X4(A0),D0
	JSR	CALCUL
	BRA.S	DONE

D6_WIN	;Y1>Y2	CAS 3 ET 4
	LEA	TAB2,A3
	MOVE	4(A0),D0
	MOVE	(A0),D2
	MOVE	2(A0),D3
	MOVE	6(A0),D1
	JSR	CALCUL

	MOVE	4+2(A0),D6	Y2
	CMP	8+2(A0),D6	Y3,Y2
	BGT.S	D6_WINB
;CAS 4	Y3>Y2
	LEA	TAB,A3
	MOVE	4(A0),D0
	MOVE	6(A0),D1
	MOVE	8(A0),D2
	MOVE	10(A0),D3
	JSR	CALCUL

	LEA	TAB,A3
	MOVE	X3(A0),D0
	MOVE	X4(A0),D2
	MOVE	Y3(A0),D1
	MOVE	Y4(A0),D3
	JSR	CALCUL

	LEA	TAB2,A3
	MOVE	X1(A0),D0
	MOVE	X4(A0),D2
	MOVE	Y1(A0),D1
	MOVE	Y4(A0),D3
	JSR	CALCUL
	BRA.S	DONE

D6_WINB	;Y2>Y3	CAS 3
	LEA	TAB2,A3
	MOVE	4(A0),D2
	MOVE	10(A0),D1
	MOVE	8(A0),D0
	MOVE	6(A0),D3
	JSR	CALCUL

	LEA	TAB,A3
	MOVE	X3(A0),D0
	MOVE	Y4(A0),D2
	MOVE	Y3(A0),D1
	MOVE	Y4(A0),D3
	JSR	CALCUL

	LEA	TAB,A3
	MOVE	X4(A0),D0
	MOVE	Y1(A0),D2
	MOVE	Y4(A0),D1
	MOVE	Y1(A0),D3
	JSR	CALCUL

DONE